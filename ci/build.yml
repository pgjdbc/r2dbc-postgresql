---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: openjdk
    tag: 8-jdk-alpine

inputs:
- name: r2dbc-postgresql

outputs:
- name: distribution-repository

caches:
- path: maven

run:
  path: r2dbc-postgresql/ci/build.sh
